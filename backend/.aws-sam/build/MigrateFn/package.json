{
  "name": "nett-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Tower5 backend Lambdas. Most of AWS SDK v3 is provided by the nodejs24.x runtime; only the S3 presigner (not in the runtime) is bundled here.",
  "type": "commonjs",
  "dependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.600.0",
    "@aws-sdk/client-s3": "^3.600.0",
    "@aws-sdk/client-transcribe": "^3.600.0",
    "@aws-sdk/s3-request-presigner": "^3.600.0"
  }
}
